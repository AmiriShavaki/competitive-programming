#include <bits/stdc++.h>
using namespace std;

#define N
//#define int long long

int a[N];

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int q;
    cin >> q;
    while(q--) {
        int n;
        cin >> n;
        for (int i = 0; i < n; i++) cin >> a[i];
    }
    return 0;
}